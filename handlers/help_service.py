from aiogram import Router, types, F
from aiogram.filters import Command

router = Router()

@router.message(F.text == "❓ Помощь")
@router.message(Command("help"))
async def cmd_help(message: types.Message):
    help_text = """
    🤖 *Помощь по боту*

    *Основные команды:*
    /start - Запустить бота
    /help - Эта справка
    /prices - Текущие курсы криптовалют

    *Что умеет бот:*
    📊 *Курсы криптовалют* - актуальные цены BTC, ETH и других криптовалют
    🔄 *Автообновление* - данные обновляются каждую минуту
    📈 *Изменение за 24ч* - показывает рост/падение в процентах

    *Как пользоваться:*
    1. Нажмите кнопку *📊 Курсы криптовалют*
    2. Получите текущие курсы
    3. Используйте кнопку *Обновить курсы* для принудительного обновления
    4. Кнопка *Назад в меню* вернет в главное меню

    *Техническая информация:*
    • Данные: CoinGecko API + Binance (резервный источник)
    • Обновление: каждые 60 секунд
    • Отслеживается: 10 популярных криптовалют

    *Если что-то не работает:*
    1. Попробуйте нажать *Обновить курсы*
    2. Подождите минуту (возможно обновление данных)
    3. Напишите разработчику если проблема повторяется
     """

    await message.answer(
            help_text,
            parse_mode="Markdown",
            disable_web_page_preview=True
        )

@router.message(F.text == "Назад в меню")
async def back_to_menu_handler(message: types.Message):
        from keyboards.keyboard import get_main_keyboard
        await message.answer(
            "Главное меню:",
            reply_markup=get_main_keyboard()
        )